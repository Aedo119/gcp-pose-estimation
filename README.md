# gcp-pose-estimation
building a machine learning pipeline that takes an aerial image crop as input and performs two tasks simultaneously: Keypoint Localization (Pose Estimation), Shape Classification
